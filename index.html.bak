<!doctype html>
<html lang="zh-Hant">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>感覺統合｜拍手節奏遊戲</title>
    <style>
      :root { color-scheme: dark; }
      body {
        margin: 0;
        font-family: system-ui, -apple-system, Segoe UI, Roboto, Noto Sans, "PingFang TC", "Microsoft JhengHei", sans-serif;
        background:#0b1020; color:#f4f7ff;
      }
      .app { max-width: 1180px; margin: 24px auto; padding: 16px; }

      .card {
        background: #121936;
        border: 1px solid #243065;
        border-radius: 16px;
        padding: 16px;
        box-shadow: 0 10px 30px rgba(0,0,0,.25);
      }

      /* 兩欄：預設固定左右，不換行；螢幕很窄才堆疊 */
      .row { display:flex; gap:16px; flex-wrap: nowrap; align-items:flex-start; }
      .col { flex: 1 1 0; min-width: 0; }
      .left-col, .right-col { width: 50%; }
      @media (max-width: 960px) {
        .row { flex-wrap: wrap; }
        .left-col, .right-col { width: 100%; }
      }

      /* 讓攝影機與覆蓋畫布不超出容器，等比縮放 */
      .card video,
      .card canvas {
        width: 100%;
        height: auto;         /* 依寬度等比縮放 */
        display: block;
        border-radius: 12px;
      }
      /* 如果你想固定視窗比例（避免太高）：打開下面兩行 */
      /* .card video { aspect-ratio: 4 / 3; object-fit: cover; } */
      /* .card canvas { aspect-ratio: auto; } */

      /* 其他小樣式（和前面保持一致） */
      .btn { background:#3a60ff; border:none; color:white; padding:10px 16px; border-radius:10px; cursor:pointer; font-weight:700; }
      .btn[disabled] { opacity:.45; cursor:not-allowed; }
      .ghost { background:#263164; }
      .pill { background:#1b2349; padding:6px 10px; border-radius:999px; font-size:13px; border:1px solid #2b3874;}
      .meter { height:10px; background:#1b2349; border-radius:999px; overflow:hidden; border:1px solid #2b3874;}
      .meter > div { height:100%; background:#3a60ff; width:0%; transition:width .2s; }
      .stage { display:flex; justify-content:center; align-items:center; min-height: 280px; }
      .clown-img { width: 360px; max-width: 100%; transition: transform .12s ease; }
      .clown-hitL { transform: translate(-6px, 3px) rotate(-6deg) scale(1.02); }
      .clown-hitR { transform: translate( 6px, 3px) rotate( 6deg) scale(1.02); }
      .downbeat   { filter: drop-shadow(0 0 12px #ffd66b); }
    </style>
  </head>
  <body>
    <div id="root" class="app"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
